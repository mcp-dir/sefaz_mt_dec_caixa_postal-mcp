# Ferramentas

SEFAZ MT DEC: Caixa Postal expõe 1 ferramenta.

### 1. `sefaz_mt_dec_caixa_postal_consultar`
**Input**: `pkcs12_cert`, `pkcs12_pass`, `cpf` (opcional), `cnpj` (opcional), `ie` (opcional), `ignora_nao_lidas` (opcional), `pagina` (opcional)

SEFAZ MT DEC: Caixa Postal, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SEFAZ MT DEC: Caixa Postal
```
